import PropTypes from "prop-types";

export let WnBarPropType = {
  value: PropTypes.number,
  unit: PropTypes.any,
  large: PropTypes.bool,
  small: PropTypes.bool
}

export let WnBarDefaultPropType = {
  value: -1,
  large: false,
  small: false
}
